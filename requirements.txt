Faker==37.11.0
fastapi==0.118.2
fastparquet==2024.11.0
httpx==0.27.0
networkx==3.5
numpy==2.3.3
pandas==2.3.3
uvicorn==0.37.0

# It's good practice to list only the top-level dependencies.
# The other packages (like anyio, pydantic, starlette, etc.)
# are dependencies of the packages listed above and will be
# installed automatically by pip.
